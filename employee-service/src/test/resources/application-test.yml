spring.jpa.hibernate.ddl-auto: none

spring:
  datasource:
    schema: classpath*:db/hsql/schema.sql
    data: classpath*:db/hsql/data.sql
  profiles:
    active: test

logging.level.org.springframework: INFO

